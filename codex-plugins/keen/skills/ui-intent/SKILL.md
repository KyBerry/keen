---
name: ui-intent
description: Reconcile a screen with its likely user job. Use when the user wants an intent walk, hierarchy diagnosis, task-fit critique, or prioritized changes for a Keen run or loose PNG.
---

# UI Intent

Read `../keen/SKILL.md` first, then `../keen/references/screen-intent.md`.

Require exactly one local run directory or PNG. Invoke `keen intent` with the safely quoted path. Use stated intent when available; otherwise label a reasonable inference and continue unless ambiguity would materially change the result.

For a run, cite only selected stable component evidence. For a loose screenshot, refer to visible regions and labels; there is no report, component index, or predicate ID to cite. Return three prioritized changes and falsifiable trip points.
