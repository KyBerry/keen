---
name: ui-deslop
description: Find measurable AI-default UI patterns and page-specific escape moves. Use when the user says a surface looks AI-generated, generic, templated, slop-like, over-carded, over-rounded, or visually interchangeable.
---

# UI De-slop

Read `../keen/SKILL.md` first, then `../keen/references/characterize.md`.

- For an HTTP(S) target, invoke `keen review` with only its accepted options; the review pipeline writes slop artifacts.
- For an existing run directory, invoke `keen slop` with only its accepted options.

Safely quote every dynamic value. Read `slop.md` first. Report the emitted score and band plus no more than three highest-contribution fingerprints, each with compact measured evidence and a page-specific escape move. Do not invent fingerprints or present this lens as a complete usability review.
